package answers

object LogicAndCodes {
    def and(left: Boolean, right: Boolean) = false
    def or(left: Boolean, right: Boolean) = false
    def nand(left: Boolean, right: Boolean) = false
    def nor(left: Boolean, right: Boolean) = false
    def xor(left: Boolean, right: Boolean) = false
    def impl(left: Boolean, right: Boolean) = false
    def equ(left: Boolean, right: Boolean) = false

    def gray(n: Int) = {
    }

    def huffman(tuples: List[Tuple2[String, Int]]) = {
        List()
    }
}