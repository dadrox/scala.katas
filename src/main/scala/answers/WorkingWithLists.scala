package answers

object WorkingWithLists {

    def last[A](in: List[A]) = {
    }

    def penultimate[A](in: List[A]) = {
    }

    def nth[A](n: Int, in: List[A]) = {
    }

    def size[A](in: List[A]) = {
    }

    def reverse[A](in: List[A]) = {
    }

    def isPalindrome[A](in: List[A]) = {
        false
    }

    def flatten[A](in: List[A]) = {
    }

    def compress[A](in: List[A]) = {
    }

    def pack[A](in: List[A]) = {
    }

    def encode[A](in: List[A]) = {
    }

    def encodeModified[A](in: List[A]) = {
    }

    def decode[A](in: List[A]) = {
    }

    def encodeDirect[A](in: List[A]) = {
    }

    def duplicate[A](in: List[A]) = {
    }

    def duplicateN[A](n: Int, in: List[A]) = {
    }

    def drop[A](n: Int, in: List[A]) = {
    }

    def split[A](length: Int, in: List[A]) = {
    }

    def slice[A](i: Int, k: Int, in: List[A]) = {
    }

    def rotate[A](n: Int, in: List[A]) = {
    }

    def removeAt[A](k: Int, in: List[A]) = {
    }

    def insertAt[A](element: A, position: Int, in: List[A]) = {
    }

    def range(lower: Int, upper: Int) = {
    }

    def range(range: Range) = {
    }

    def randomSelect[A](howMany: Int, in: List[A]) = {
        List[A]()
    }

    def lotto(n: Int, m: Int) = {
        List[Int]()
    }

    def randomPermute[A](in: List[A]) = {
        List[A]()
    }

    def combinations[A](k: Int, in: List[A]) = {
        List()
    }

    def group3[A](in: List[A]) = {
        List()
    }

    def group[A](groups: List[Int], in: List[A]) = {
        List()
    }

    def lsort[A](in: List[A]) = {
    }

    def lsortFreq[A](in: List[A]) = {
    }
}